ansible

Enable password based authentication in AWS

Change the PasswordAuthentication to yes in file 



cd /etc/ssh/sshd_config.d/ 

60-cloudimg-settings.conf


restart the sshd



sudo systemctl daemon-reload
sudo systemctl restart ssh

cd ~

sudo useradd devops or sudo adduser devops

sudo passwd devops


sudo visudo


ansible installing

sudo apt update
sudo apt install software-properties-common
sudo add-apt-repository --yes --update ppa:ansible/ansible
sudo apt install ansible -y






ansible-playbook -i hosts --syntax-check website.yaml

ansible-playbook -i hosts website.yaml




